export const customStyles = {
  control: (defaultStyles) => ({
    ...defaultStyles,
    width: "fit-content",
    minWidth: "120px",
    padding: "8px",
  }),
  option: (defaultStyles) => ({
    ...defaultStyles,
    color: "black",
  }),
};
